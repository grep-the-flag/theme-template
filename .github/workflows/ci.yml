name: CI
on:
  pull_request:
  push:
    branches: [main, develop]
jobs:
  ci:
    name: ci
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI placeholder - replaced by SDK reusable workflows (ADR-0013)."
